const partnerForm=document.querySelector('#partnerForm');partnerForm.onsubmit=async e=>{e.preventDefault();const types=[...partnerForm.querySelectorAll('[name="propertyTypes"]:checked')].map(x=>x.value);if(!types.length){alert('主な取扱物件を1つ以上選択してください');return}const btn=partnerForm.querySelector('.submit');btn.disabled=true;btn.textContent='送信しています…';const data=Object.fromEntries(new FormData(partnerForm));data.propertyTypes=types;data.id='PT-'+new Date().toISOString().slice(0,10).replaceAll('-','')+'-'+Math.random().toString(36).slice(2,8).toUpperCase();data.createdAt=new Date().toISOString();try{if(!window.GAS_URL||window.GAS_URL.includes('ここに'))throw Error('送信先が未設定です');await fetch(window.GAS_URL,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(data)});document.querySelector('#partnerCard').innerHTML=`<div class="partner-complete"><div>✓</div><h2>お問い合わせを受け付けました</h2><p>受付番号：<b>${data.id}</b></p><p>内容を確認のうえ、運営事務局よりご連絡します。</p><a href="index.html">トップページへ戻る</a></div>`}catch(err){alert(err.message||'送信できませんでした');btn.disabled=false;btn.textContent='入力内容を送信する'}};
